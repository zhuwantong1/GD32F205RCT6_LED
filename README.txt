**	这个工程可以点亮PC14的LED灯，RESET（及低电平）点亮，
**	延时函数的解决，两种方式，第一次提交是中断模式，要在gd32f20x_it.c进行中断减减操作。第二种是轮询模式，（正点原子使用的就是这种），通过控制SysTick的寄存器来进行循环，一直到计数器清零才结束，这里要注意如果选用的是systick_clksource_set(SYSTICK_CLKSOURCE_HCLK_DIV8);fac_us = SystemCoreClock/8000000;这里的8000000是必须的，为了凑与us相对应的因子，及当SysTick时钟频率是120M/8=15M时，那么完成1次的定时（每个计数周期）需要1/15us，所以要想让定时为1us的情况下，那就需要15个定时周期，这15个定时周期就是从fac_us = SystemCoreClock/8000000;这句话里得到的。fac_ms = fac_us *1000;毫秒也是如此。
**	还要在gd32f20x.h中进行时钟频率的修改，将25000000改为8000000